# Core FastAPI Framework
# fastapi==0.111.0  # Specify a recent stable version
fastapi>=0.110.0,<0.111.0
# Add psycopg2 for PostgreSQL support
psycopg2-binary>=2.9.9,<3.0.0 
uvicorn[standard]==0.29.0 # Specify a recent stable version
pydantic-settings==2.2.1
python-multipart==0.0.9 # For form data
jinja2==3.1.4 # For templates if needed by FastAPI/Starlette
fastapi-limiter>=0.1.6,<0.2.0 # Add dependency for rate limiting

# Web Server (Optional but common)
gunicorn==22.0.0  # Updated from 20.1.0

# Security & Auth
PyJWT==2.8.0  # Updated from 2.6.0
passlib[bcrypt]==1.7.4 # For password hashing (replaces standalone bcrypt)
# Add slowapi if rate limiting is needed later: # slowapi==0.1.9

# NLP & ML Components
spacy==3.7.4 # Keep as is for now, check compatibility if issues arise
transformers==4.41.0 # Updated from 4.28.1
tokenizers==0.19.1 # Updated from 0.13.3 (often needed with transformers update)
torch==2.2.2 # Updated from 2.2.0 (CPU or specific CUDA version might be needed depending on deployment)
scikit-learn==1.3.0 # Updated from 1.2.2
sentence-transformers==2.7.0 # Updated from 2.2.2

# HTTP Client
requests==2.31.0 # Updated from 2.28.2

# LLM SDKs
anthropic==0.27.0 # Pinned from >=0.5.0 to recent version

# Utilities
python-dateutil==2.9.0.post0 # Updated from 2.8.2
Markdown==3.6 # Updated from 3.4.3
email-validator==2.1.1 # Added for Pydantic email validation

# Database (Assuming SQLite for now)
# sqlalchemy==2.0.30 # Add if using SQLAlchemy ORM later
# alembic==1.13.1 # For migrations if using SQLAlchemy

# Development/Testing
# pytest==8.2.2
# pytest-asyncio==0.23.7
# httpx==0.27.0 # For testing async FastAPI clients

# --- Removed Packages ---
# Flask==2.2.3
# flask-cors==3.0.10
# flask-swagger-ui==4.11.1
# Werkzeug==2.2.3
# python-dotenv==1.0.0 (replaced by pydantic-settings)
# flask-limiter==3.3.1
# bcrypt==4.3.0 (included via passlib[bcrypt])
