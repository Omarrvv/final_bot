name: egypt-tourism1
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - numpy
  - scipy
  - pandas
  - scikit-learn=1.5.0
  - spacy
  - sqlite
  - pip:
      # The pip installs will be managed via requirements.txt
      - -r requirements.txt
prefix: /opt/miniconda3/envs/egypt-tourism1
